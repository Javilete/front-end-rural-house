define([

],
    function() {

        var rhContactUsController = function($scope){

            $scope.contactObj = {};

            $scope.send = function() {

            }

        }

        rhContactUsController.$inject = ['$scope'];

        return rhContactUsController;
    }
)